{{!< ../default}}

<link rel="stylesheet" type="text/css" href="{{asset "custom/bootstrap.min.css"}}" />

<header class="site-header outer {{#if @blog.cover_image}}" style="background-image: url({{@blog.cover_image}}){{else}}no-cover{{/if}}">
    <div class="inner">
        <div class="site-header-content">
            <h1 class="site-title">
                {{#if @blog.logo}}
                    <img class="site-logo" src="{{@blog.logo}}" alt="{{@blog.title}}" />
                {{else}}
                    {{@blog.title}}
                {{/if}}
            </h1>
            <h2 class="site-description">{{@blog.description}}</h2>
        </div>
        {{> "site-nav"}}
    </div>
</header>

{{!-- The main content area --}}
<main id="site-main" class="site-main outer" role="main">
    <div class="inner">

        <h1>Infos zu unserern Büchern</h1>

    {{#if isSuccess}}
        <div>
            Wir haben Dir eine Nachricht an die Adresse {{email}} gesendet. Falls Du nichts bekommst, prüfe bitte Deinen Spam-Ordner.
        </div>
    {{else}}

    <p>
        Hier kannst Du zusätzliche Informationen zu unseren beiden Büchern zu AngularJS 1.x und Angular (2+) bei O'Reilly anfordern. Zu diesen Informationen gehören die Quellcodebeispiele und Errata.
    </p>


    <form method="post">

        {{#if hasValidationError}}
            <p style="color: red">
                Bitte kontrolliere Deine Eingaben.
            </p>
        {{/if}}
        {{#if hasSendError}}
            <p style="color: red">
                Fehler beim Versenden der Nachricht. Bitte überprüfe die angegebene Email-Adresse.
            </p>
        {{/if}}
        <div class="form-group">
            <label>EMail-Adresse *</label>
            <input class="form-control" value="{{email}}" name="email">
        </div>
        <div class="form-group">
            <label>Erstes Wort von Buchseite 100 (egal, welches der beiden Bücher)*</label>
            <input class="form-control" value="{{word}}" name="word">
        </div>
        <div class="form-group">
            <div class="form-check">
            <label class="form-check-label">
                <input checked name="notify" class="form-check-input" type="checkbox" value="1">
                Ich möchte über Neuerungen informiert werden (Newsletter, max. 4x pro Jahr).
            </label>
            </div>        
        </div>
        <div class="form-group">
            <input type="submit" value="Senden" class="btn btn-primary">
        </div>

        {{#if isRequest}}
        <input type="hidden" name="isRequest" value="1">
        {{/if}}


    </form>
    
    {{/if}}
        <p>&nbsp;</p>
        <p>&nbsp;</p>
    </div>
</main>